# Google Sign-In client id (exposed to the browser)
VITE_GOOGLE_CLIENT_ID=

# Optional: only set if the API is hosted on a different origin than the client.
# Leave empty in dev — Vite proxies /api to the server (see vite.config.ts).
# VITE_API_URL=
