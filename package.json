{
  "name": "duluwa-art",
  "version": "1.0.0",
  "private": true,
  "description": "Duluwa Art Gallery — React (Vite) client + Node.js (Express) API server.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server start",
    "install:all": "npm --prefix server install && npm --prefix client install",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "db:seed": "npm --prefix server run db:seed",
    "db:push": "npm --prefix server run db:push",
    "db:studio": "npm --prefix server run db:studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
